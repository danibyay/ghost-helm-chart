apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "my-ghost-chart.fullname" . }}-test-connection"
  labels:
    {{- include "my-ghost-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "my-ghost-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
